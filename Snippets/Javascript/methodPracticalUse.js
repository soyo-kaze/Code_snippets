const nameSpeak = {};

nameSpeak.name = "My Name is Vaibhav"; 

nameSpeak.speak = console.log(nameSpeak.name); //Basically Added Function inside an object.



consol.log(nameSpeak); //This shows whats inside the object.
